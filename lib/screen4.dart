import 'package:flutter/material.dart';

class ShopPlan extends StatelessWidget {
  const ShopPlan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      appBar: AppBar(
        centerTitle: true,
        title: Text('ICICI SHOP PLAN'),
      ),
    );
  }
}
